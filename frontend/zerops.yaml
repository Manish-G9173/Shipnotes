zerops:
  - setup: frontend
    build:
      base: nodejs@20
      buildCommands:
        - npm install
      deployFiles:
        - .
    run:
      base: nodejs@20
      start: npm start
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: 3000
